server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medilabo
    username: root
    password: "*!Robinhood974!*"
    platform: mysql
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE